# spring.application.name=Extra2Essentials

# # Server
# server.port=8080

# # Database
# spring.datasource.url=jdbc:mysql://localhost:3306/extra2essential
# spring.datasource.username=root
# spring.datasource.password=Siy@r@m5
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# # JWT
# jwt.secret=extra2essential_super_secret_key_2024_hackathon
# jwt.expiration=86400000

# # Mail
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=malewarpallavi@gmail.com
# spring.mail.password=upuicdxvcfuxcajt
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true





spring.application.name=Extra2Essentials

# Server
server.port=${PORT:8080}

# Database - works both locally and Railway
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/extra2essential}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Siy@r@m5}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secret=${JWT_SECRET:extra2essential_super_secret_key_2024_hackathon}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:malewarpallavi@gmail.com}
spring.mail.password=${MAIL_PASSWORD:upuicdxvcfuxcajt}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true